# screenshot

Screenshots of the working PayGuard demo — web dashboard, mobile app, Signal Lab, and real-time alerts.

Found in the git repo as `.png`/`.jpg` (or as links from `local/` during the build). Naming convention: `web-<view>.png`, `mobile-<screen>.png`, e.g. `web-dashboard.png`, `mobile-threat-center.png`.